package org.example.LexerImplementation;

// Token types
public enum TokenType {
    NUMBER,
    IDENTIFIER,
    OPERATOR,
    LPAREN, // (
    RPAREN, // )
    LBRACE, // {
    RBRACE, // }
    IF,
    ELSE,
    EQUALS, // ==
    ASSIGN, // =
    SEMICOLON, // ;
    MULTIPLY, // *
    DIVIDE // /
}
